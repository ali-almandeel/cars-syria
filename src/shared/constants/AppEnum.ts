export enum ThemeStyleRadius {
    MODERN = 30,
    STANDARD = 16,
}

export enum LayoutDirection {
    RTL = "rtl",
    LTR = "ltr",
}

export enum LayoutLanguage {
    AR = "ar-SA",
    EN = "en-US",
}

export enum ThemeMode {
    LIGHT = "light",
    DARK = "dark",
}

export enum Fonts {
    LIGHT = "300",
    REGULAR = "400",
    MEDIUM = "500",
    SEMI_BOLD = "600",
    BOLD = "700",
}

export enum textLight {
    primary = "rgb(17, 24, 39)",
    secondary=  "rgb(107, 114, 128)",
    disabled = "rgb(149, 156, 169)",
};