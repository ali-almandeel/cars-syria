import { FC } from "react"

interface IAppNavbarProps {}
const AppNavbar:FC<IAppNavbarProps> = (props) => {
  const {} = props;
  return (
    <div>AppNavbar</div>
  )
}

export default AppNavbar